using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MORPG_Player : MonoBehaviour
{
    // TODO (장현명) : 아예 따로 컨텐츠를 만들지 결정 후 
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
